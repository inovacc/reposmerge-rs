//! reposmerge CLI entry point. Ported last (depends on all library modules).
//! Placeholder until the `cmd/reposmerge` port-unit lands.

fn main() {
    eprintln!("reposmerge: CLI not yet ported");
    std::process::exit(1);
}
